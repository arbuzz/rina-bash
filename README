В репозитории есть файл run - это sh скрипт, который проходит по всем папкам на текущем уровне, и проверяет, есть ли там файл vasp.out и есть ли в нем слово required. Есть также несколько папок для проверки.
